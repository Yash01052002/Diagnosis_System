"""Persistence layer (repository pattern).

Import submodules directly, e.g. ``from app.repositories.crash import
CrashReportRepository``. See :mod:`app.services` for why this package does not
re-export.
"""
