"""AI diagnosis and retrieval-augmented generation.

Everything external is behind an interface with a deterministic, offline
default, so the pipeline runs and is tested without a network or an API key.
"""
